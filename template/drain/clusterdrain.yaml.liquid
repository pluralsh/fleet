apiVersion: deployments.plural.sh/v1alpha1
kind: ClusterDrain
metadata:
  {% raw %}name: drain-{{ configuration.version }}{% endraw %}
spec:
  flowControl:
    maxConcurrency: 10
  labelSelector:
    matchLabels:
      platform.plural.sh/drainable: "true"