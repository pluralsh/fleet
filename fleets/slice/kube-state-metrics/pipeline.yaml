apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: slice-kube-state-metrics
spec:
  projectRef:
    name: slice
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: slice
          criteria:
            prAutomationRef:
              name: slice-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: slice
          criteria:
            prAutomationRef:
              name: slice-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL