apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: tf-test-kube-state-metrics
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: tf-test
          criteria:
            prAutomationRef:
              name: tf-test-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: tf-test
          criteria:
            prAutomationRef:
              name: tf-test-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL