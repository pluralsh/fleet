apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: another-demo-kube-state-metrics
spec:
  projectRef:
    name: another-demo
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: another-demo
          criteria:
            prAutomationRef:
              name: another-demo-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: another-demo
          criteria:
            prAutomationRef:
              name: another-demo-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL