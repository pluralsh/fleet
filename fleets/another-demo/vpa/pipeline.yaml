apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: another-demo-vpa
spec:
  projectRef:
    name: another-demo
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: another-demo
          criteria:
            prAutomationRef:
              name: another-demo-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: another-demo
          criteria:
            prAutomationRef:
              name: another-demo-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL