apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-test-fleet-vpa
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-test-fleet
          criteria:
            prAutomationRef:
              name: gke-test-fleet-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-test-fleet
          criteria:
            prAutomationRef:
              name: gke-test-fleet-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL