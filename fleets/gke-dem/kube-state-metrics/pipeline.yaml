apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-dem-kube-state-metrics
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-dem
          criteria:
            prAutomationRef:
              name: gke-dem-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-dem
          criteria:
            prAutomationRef:
              name: gke-dem-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL