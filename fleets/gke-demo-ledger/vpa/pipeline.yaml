apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-demo-ledger-vpa
spec:
  projectRef:
    name: gke-demo-ledger
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-demo-ledger
          criteria:
            prAutomationRef:
              name: gke-demo-ledger-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-demo-ledger
          criteria:
            prAutomationRef:
              name: gke-demo-ledger-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL