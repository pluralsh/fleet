apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: lke-fleet-kube-state-metrics
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: lke-fleet
          criteria:
            prAutomationRef:
              name: lke-fleet-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: lke-fleet
          criteria:
            prAutomationRef:
              name: lke-fleet-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL