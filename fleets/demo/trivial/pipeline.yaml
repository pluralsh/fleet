apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: demo-trivial
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: trivial-dev
            namespace: demo
          criteria:
            prAutomationRef:
              name: demo-trivial
    - name: prod
      services:
        - serviceRef:
            name: trivial-prod
            namespace: demo
          criteria:
            prAutomationRef:
              name: demo-trivial
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL