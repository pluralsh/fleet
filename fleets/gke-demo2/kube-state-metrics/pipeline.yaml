apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-demo2-kube-state-metrics
spec:
  projectRef:
    name: gke-demo2
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-demo2
          criteria:
            prAutomationRef:
              name: gke-demo2-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-demo2
          criteria:
            prAutomationRef:
              name: gke-demo2-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL