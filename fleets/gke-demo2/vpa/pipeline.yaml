apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-demo2-vpa
spec:
  projectRef:
    name: gke-demo2
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-demo2
          criteria:
            prAutomationRef:
              name: gke-demo2-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-demo2
          criteria:
            prAutomationRef:
              name: gke-demo2-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL