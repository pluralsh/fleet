apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: testtest-kube-state-metrics
spec:
  projectRef:
    name: testtest
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: testtest
          criteria:
            prAutomationRef:
              name: testtest-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: testtest
          criteria:
            prAutomationRef:
              name: testtest-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL