apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: john-demo-kube-state-metrics
spec:
  projectRef:
    name: john-demo
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: john-demo
          criteria:
            prAutomationRef:
              name: john-demo-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: john-demo
          criteria:
            prAutomationRef:
              name: john-demo-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL