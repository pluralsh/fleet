apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: john-cluster-demo-vpa
spec:
  projectRef:
    name: john-cluster-demo
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: john-cluster-demo
          criteria:
            prAutomationRef:
              name: john-cluster-demo-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: john-cluster-demo
          criteria:
            prAutomationRef:
              name: john-cluster-demo-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL