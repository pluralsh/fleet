apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-fleet-1-kube-state-metrics
spec:
  projectRef:
    name: gke-fleet-1
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-fleet-1
          criteria:
            prAutomationRef:
              name: gke-fleet-1-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-fleet-1
          criteria:
            prAutomationRef:
              name: gke-fleet-1-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL