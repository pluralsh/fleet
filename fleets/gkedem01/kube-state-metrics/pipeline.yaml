apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gkedem01-kube-state-metrics
spec:
  projectRef:
    name: gkedem01
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gkedem01
          criteria:
            prAutomationRef:
              name: gkedem01-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gkedem01
          criteria:
            prAutomationRef:
              name: gkedem01-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL