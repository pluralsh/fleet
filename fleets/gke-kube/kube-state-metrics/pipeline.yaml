apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-kube-kube-state-metrics
spec:
  projectRef:
    name: gke-kube
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-kube
          criteria:
            prAutomationRef:
              name: gke-kube-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-kube
          criteria:
            prAutomationRef:
              name: gke-kube-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL