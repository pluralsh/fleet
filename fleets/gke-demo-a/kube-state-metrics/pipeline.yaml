apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-demo-a-kube-state-metrics
spec:
  projectRef:
    name: gke-demo-a
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-demo-a
          criteria:
            prAutomationRef:
              name: gke-demo-a-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-demo-a
          criteria:
            prAutomationRef:
              name: gke-demo-a-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL