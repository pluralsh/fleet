apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-demo-sh-vpa
spec:
  projectRef:
    name: gke-demo-sh
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-demo-sh
          criteria:
            prAutomationRef:
              name: gke-demo-sh-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-demo-sh
          criteria:
            prAutomationRef:
              name: gke-demo-sh-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL