apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-demo-sh-kube-state-metrics
spec:
  projectRef:
    name: gke-demo-sh
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-demo-sh
          criteria:
            prAutomationRef:
              name: gke-demo-sh-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-demo-sh
          criteria:
            prAutomationRef:
              name: gke-demo-sh-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL