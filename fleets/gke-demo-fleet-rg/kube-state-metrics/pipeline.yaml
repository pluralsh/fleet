apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-demo-fleet-rg-kube-state-metrics
spec:
  projectRef:
    name: gke-demo-fleet-rg
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-demo-fleet-rg
          criteria:
            prAutomationRef:
              name: gke-demo-fleet-rg-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-demo-fleet-rg
          criteria:
            prAutomationRef:
              name: gke-demo-fleet-rg-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL