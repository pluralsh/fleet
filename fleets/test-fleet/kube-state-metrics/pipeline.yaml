apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: test-fleet-kube-state-metrics
spec:
  projectRef:
    name: test-fleet
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: test-fleet
          criteria:
            prAutomationRef:
              name: test-fleet-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: test-fleet
          criteria:
            prAutomationRef:
              name: test-fleet-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL