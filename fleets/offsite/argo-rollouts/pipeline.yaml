apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: offsite-argo-rollouts
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: argo-rollouts-dev
            namespace: offsite
          criteria:
            prAutomationRef:
              name: offsite-pra
    - name: prod
      services:
        - serviceRef:
            name: argo-rollouts-prod
            namespace: offsite
          criteria:
            prAutomationRef:
              name: offsite-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL