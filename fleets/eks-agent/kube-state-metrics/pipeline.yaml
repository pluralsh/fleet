apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: eks-agent-kube-state-metrics
spec:
  projectRef:
    name: eks-agent
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: eks-agent
          criteria:
            prAutomationRef:
              name: eks-agent-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: eks-agent
          criteria:
            prAutomationRef:
              name: eks-agent-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL