apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: eks-agent-vpa
spec:
  projectRef:
    name: eks-agent
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: eks-agent
          criteria:
            prAutomationRef:
              name: eks-agent-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: eks-agent
          criteria:
            prAutomationRef:
              name: eks-agent-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL