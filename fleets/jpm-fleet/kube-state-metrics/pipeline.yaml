apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: jpm-fleet-kube-state-metrics
spec:
  projectRef:
    name: jpm-fleet
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: jpm-fleet
          criteria:
            prAutomationRef:
              name: jpm-fleet-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: jpm-fleet
          criteria:
            prAutomationRef:
              name: jpm-fleet-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL