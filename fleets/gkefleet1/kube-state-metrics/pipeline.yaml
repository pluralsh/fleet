apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gkefleet1-kube-state-metrics
spec:
  projectRef:
    name: gkefleet1
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gkefleet1
          criteria:
            prAutomationRef:
              name: gkefleet1-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gkefleet1
          criteria:
            prAutomationRef:
              name: gkefleet1-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL