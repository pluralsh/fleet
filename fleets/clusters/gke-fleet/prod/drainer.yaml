apiVersion: deployments.plural.sh/v1alpha1
kind: GlobalService
metadata:
  name: -prod-drainer
spec:
  projectRef:
    name: 
  tags:
    tier: prod
  template:
    name: drainer
    namespace: plrl-deploy-operator
    git:
      folder: services/drainer
      ref: main
    configuration:
      version: ""
    repositoryRef:
      kind: GitRepository
      name: fleet
      namespace: fleets