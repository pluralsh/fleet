apiVersion: deployments.plural.sh/v1alpha1
kind: InfrastructureStack
metadata:
  name: gke-demo-demo-gke-dev
spec:
  
  agentId: fl1md7ihajlxdd8c
  
  name: gke-demo-demo-gke-dev
  type: TERRAFORM
  approval: true
  detach: false
  manageState: true
  actor: gke-demo-sa@plural.sh
  projectRef:
    name: gke-demo
  configuration:
    version: 1.8.2
  repositoryRef:
    name: fleet
    namespace: fleets
  clusterRef:
    name: mgmt
    namespace: infra
  git:
    ref: main
    folder: terraform/gke-cluster
  files:
  - mountPath: /plural
    secretRef:
      name: gcp-creds
  variables:
    kubernetes_version: "{{ configuration.kubernetesVersion }}"
    drain_node_group: "{{ configuration.drain }}"
    active_node_group: "{{ configuration.active }}"
    next_kubernetes_version: "{{ configuration.nextKubernetesVersion }}"
    cluster: demo-gke-dev
    tier: dev
    fleet: gke-demo
  environment:
  - name: GOOGLE_APPLICATION_CREDENTIALS
    value: /plural/creds.json