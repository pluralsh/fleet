apiVersion: deployments.plural.sh/v1alpha1
kind: InfrastructureStack
metadata:
  name: livedemo-cluster-dev
spec:
  name: livedemo-cluster-dev
  type: TERRAFORM
  approval: true
  detach: false
  manageState: true
  actor: livedemo-sa@plural.sh
  projectRef:
    name: livedemo
  configuration:
    version: 1.8.2
  repositoryRef:
    name: fleet
    namespace: fleets
  clusterRef:
    name: mgmt
    namespace: infra
  git:
    ref: main
    folder: terraform/gke-cluster
  files:
  - mountPath: /plural
    secretRef:
      name: gcp-creds
  variables:
    kubernetes_version: "{{ configuration.kubernetesVersion }}"
    next_kubernetes_version: "{{ configuration.nextKubernetesVersion }}"
    cluster: livedemo-dev
    tier: dev
    fleet: livedemo
  environment:
  - name: GOOGLE_APPLICATION_CREDENTIALS
    value: /plural/creds.json