apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: eks-fleet-kube-state-metrics
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: eks-fleet
          criteria:
            prAutomationRef:
              name: eks-fleet-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: eks-fleet
          criteria:
            prAutomationRef:
              name: eks-fleet-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL