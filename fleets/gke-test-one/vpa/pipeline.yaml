apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-test-one-vpa
spec:
  projectRef:
    name: gke-test-one
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-test-one
          criteria:
            prAutomationRef:
              name: gke-test-one-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-test-one
          criteria:
            prAutomationRef:
              name: gke-test-one-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL