apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: demo-test-kube-state-metrics
spec:
  projectRef:
    name: demo-test
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: demo-test
          criteria:
            prAutomationRef:
              name: demo-test-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: demo-test
          criteria:
            prAutomationRef:
              name: demo-test-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL