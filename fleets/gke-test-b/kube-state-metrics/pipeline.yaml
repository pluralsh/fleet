apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-test-b-kube-state-metrics
spec:
  projectRef:
    name: gke-test-b
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-test-b
          criteria:
            prAutomationRef:
              name: gke-test-b-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-test-b
          criteria:
            prAutomationRef:
              name: gke-test-b-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL