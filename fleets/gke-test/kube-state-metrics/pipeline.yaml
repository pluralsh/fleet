apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-test-kube-state-metrics
spec:
  projectRef:
    name: gke-test
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-test
          criteria:
            prAutomationRef:
              name: gke-test-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-test
          criteria:
            prAutomationRef:
              name: gke-test-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL