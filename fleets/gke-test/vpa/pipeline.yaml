apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-test-vpa
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-test
          criteria:
            prAutomationRef:
              name: gke-test-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-test
          criteria:
            prAutomationRef:
              name: gke-test-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL