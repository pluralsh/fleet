apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: cvent-kube-state-metrics
spec:
  projectRef:
    name: cvent
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: cvent
          criteria:
            prAutomationRef:
              name: cvent-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: cvent
          criteria:
            prAutomationRef:
              name: cvent-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL