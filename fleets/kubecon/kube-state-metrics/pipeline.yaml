apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: kubecon-kube-state-metrics
spec:
  projectRef:
    name: kubecon
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: kubecon
          criteria:
            prAutomationRef:
              name: kubecon-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: kubecon
          criteria:
            prAutomationRef:
              name: kubecon-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL