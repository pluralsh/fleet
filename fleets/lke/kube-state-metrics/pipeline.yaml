apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: lke-kube-state-metrics
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: lke
          criteria:
            prAutomationRef:
              name: lke-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: lke
          criteria:
            prAutomationRef:
              name: lke-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL