apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-agent-vpa
spec:
  projectRef:
    name: gke-agent
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-agent
          criteria:
            prAutomationRef:
              name: gke-agent-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-agent
          criteria:
            prAutomationRef:
              name: gke-agent-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL