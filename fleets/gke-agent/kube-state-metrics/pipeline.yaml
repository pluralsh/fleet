apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-agent-kube-state-metrics
spec:
  projectRef:
    name: gke-agent
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-agent
          criteria:
            prAutomationRef:
              name: gke-agent-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-agent
          criteria:
            prAutomationRef:
              name: gke-agent-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL