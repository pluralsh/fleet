apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: demofleet-kube-state-metrics
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: demofleet
          criteria:
            prAutomationRef:
              name: demofleet-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: demofleet
          criteria:
            prAutomationRef:
              name: demofleet-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL