apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: runthrough-kube-state-metrics
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: runthrough
          criteria:
            prAutomationRef:
              name: runthrough-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: runthrough
          criteria:
            prAutomationRef:
              name: runthrough-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL