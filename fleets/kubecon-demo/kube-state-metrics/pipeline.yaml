apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: kubecon-demo-kube-state-metrics
spec:
  projectRef:
    name: kubecon-demo
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: kubecon-demo
          criteria:
            prAutomationRef:
              name: kubecon-demo-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: kubecon-demo
          criteria:
            prAutomationRef:
              name: kubecon-demo-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL