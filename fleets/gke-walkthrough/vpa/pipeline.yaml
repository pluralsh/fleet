apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-walkthrough-vpa
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-walkthrough
          criteria:
            prAutomationRef:
              name: gke-walkthrough-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-walkthrough
          criteria:
            prAutomationRef:
              name: gke-walkthrough-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL