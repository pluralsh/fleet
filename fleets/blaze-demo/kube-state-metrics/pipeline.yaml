apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: blaze-demo-kube-state-metrics
spec:
  projectRef:
    name: blaze-demo
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: blaze-demo
          criteria:
            prAutomationRef:
              name: blaze-demo-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: blaze-demo
          criteria:
            prAutomationRef:
              name: blaze-demo-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL