apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: new-fleet-vpa
spec:
  projectRef:
    name: new-fleet
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: new-fleet
          criteria:
            prAutomationRef:
              name: new-fleet-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: new-fleet
          criteria:
            prAutomationRef:
              name: new-fleet-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL