apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: eks-vpa
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: eks
          criteria:
            prAutomationRef:
              name: eks-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: eks
          criteria:
            prAutomationRef:
              name: eks-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL