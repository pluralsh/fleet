apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: eks-kube-state-metrics
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: eks
          criteria:
            prAutomationRef:
              name: eks-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: eks
          criteria:
            prAutomationRef:
              name: eks-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL