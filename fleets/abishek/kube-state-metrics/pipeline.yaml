apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: abishek-kube-state-metrics
spec:
  projectRef:
    name: abishek
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: abishek
          criteria:
            prAutomationRef:
              name: abishek-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: abishek
          criteria:
            prAutomationRef:
              name: abishek-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL