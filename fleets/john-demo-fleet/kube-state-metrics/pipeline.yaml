apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: john-demo-fleet-kube-state-metrics
spec:
  projectRef:
    name: john-demo-fleet
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: john-demo-fleet
          criteria:
            prAutomationRef:
              name: john-demo-fleet-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: john-demo-fleet
          criteria:
            prAutomationRef:
              name: john-demo-fleet-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL