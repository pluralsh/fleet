apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: eks-demo-ledger-vpa
spec:
  projectRef:
    name: eks-demo-ledger
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: eks-demo-ledger
          criteria:
            prAutomationRef:
              name: eks-demo-ledger-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: eks-demo-ledger
          criteria:
            prAutomationRef:
              name: eks-demo-ledger-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL