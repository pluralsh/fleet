apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: mlx-kube-state-metrics
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: mlx
          criteria:
            prAutomationRef:
              name: mlx-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: mlx
          criteria:
            prAutomationRef:
              name: mlx-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL