apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: mlx-vpa
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: mlx
          criteria:
            prAutomationRef:
              name: mlx-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: mlx
          criteria:
            prAutomationRef:
              name: mlx-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL