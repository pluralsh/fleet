apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: mlx-argo-rollouts
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: argo-rollouts-dev
            namespace: mlx
          criteria:
            prAutomationRef:
              name: mlx-pra
    - name: prod
      services:
        - serviceRef:
            name: argo-rollouts-prod
            namespace: mlx
          criteria:
            prAutomationRef:
              name: mlx-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL