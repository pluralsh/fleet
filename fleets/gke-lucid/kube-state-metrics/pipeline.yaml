apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-lucid-kube-state-metrics
spec:
  projectRef:
    name: gke-lucid
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-lucid
          criteria:
            prAutomationRef:
              name: gke-lucid-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-lucid
          criteria:
            prAutomationRef:
              name: gke-lucid-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL