apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-lucid-vpa
spec:
  projectRef:
    name: gke-lucid
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-lucid
          criteria:
            prAutomationRef:
              name: gke-lucid-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-lucid
          criteria:
            prAutomationRef:
              name: gke-lucid-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL