apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-aws-demo-vpa
spec:
  projectRef:
    name: gke-aws-demo
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-aws-demo
          criteria:
            prAutomationRef:
              name: gke-aws-demo-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-aws-demo
          criteria:
            prAutomationRef:
              name: gke-aws-demo-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL