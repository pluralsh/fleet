apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-demo-test-vpa
spec:
  projectRef:
    name: gke-demo-test
  stages:
    - name: dev
      services:
        - serviceRef:
            name: vpa-dev
            namespace: gke-demo-test
          criteria:
            prAutomationRef:
              name: gke-demo-test-pra
    - name: prod
      services:
        - serviceRef:
            name: vpa-prod
            namespace: gke-demo-test
          criteria:
            prAutomationRef:
              name: gke-demo-test-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL