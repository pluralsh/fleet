apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-demo-test-kube-state-metrics
spec:
  projectRef:
    name: gke-demo-test
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-demo-test
          criteria:
            prAutomationRef:
              name: gke-demo-test-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-demo-test
          criteria:
            prAutomationRef:
              name: gke-demo-test-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL