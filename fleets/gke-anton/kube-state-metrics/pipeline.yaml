apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: gke-anton-kube-state-metrics
spec:
  projectRef:
    name: gke-anton
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: gke-anton
          criteria:
            prAutomationRef:
              name: gke-anton-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: gke-anton
          criteria:
            prAutomationRef:
              name: gke-anton-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL