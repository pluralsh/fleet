apiVersion: deployments.plural.sh/v1alpha1
kind: Pipeline
metadata:
  name: plrl-kube-state-metrics
spec:
  stages:
    - name: dev
      services:
        - serviceRef:
            name: kube-state-metrics-dev
            namespace: plrl
          criteria:
            prAutomationRef:
              name: plrl-pra
    - name: prod
      services:
        - serviceRef:
            name: kube-state-metrics-prod
            namespace: plrl
          criteria:
            prAutomationRef:
              name: plrl-pra
  edges:
    - from: dev
      to: prod
      gates:
      - name: approval-gate
        type: APPROVAL