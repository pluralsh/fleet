apiVersion: deployments.plural.sh/v1alpha1
kind: ServiceDeployment
metadata:
  name: offsite
spec:
  namespace: offsite
  templated: false
  git:
    folder: fleets/offsite
    ref: main
  repositoryRef:
    kind: GitRepository
    name: fleet
    namespace: fleets
  clusterRef:
    kind: Cluster
    name: mgmt
    namespace: infra