apiVersion: deployments.plural.sh/v1alpha1
kind: ServiceDeployment
metadata:
  name: demo-test
spec:
  namespace: demo-test
  git:
    folder: fleets/demo-test
    ref: main
  repositoryRef:
    kind: GitRepository
    name: fleet
    namespace: fleets
  clusterRef:
    kind: Cluster
    name: mgmt
    namespace: infra