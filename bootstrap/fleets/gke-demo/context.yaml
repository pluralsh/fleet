apiVersion: deployments.plural.sh/v1alpha1
kind: PipelineContext
metadata:
  name: gke-demo-pipeline-context
spec:
  pipelineRef:
    name: gke-demo-fleet-upgrade
    namespace: fleets
  context:
    fleet: gke-demo
    version: '1.30'