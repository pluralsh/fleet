apiVersion: deployments.plural.sh/v1alpha1
kind: ServiceDeployment
metadata:
  name: richardtest
spec:
  namespace: richardtest
  git:
    folder: fleets/richardtest
    ref: main
  repositoryRef:
    kind: GitRepository
    name: fleet
    namespace: fleets
  clusterRef:
    kind: Cluster
    name: mgmt
    namespace: infra